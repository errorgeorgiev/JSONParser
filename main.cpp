#include <iostream>
#include "JSON_Object.h"
#include "JSON_Array.h"
#include "File.h"
using namespace std;
int main() {
	/*File file;
	file.openFile("json.json");
	//cout << file.getText() << endl;
	file.validate();
	
	//cout << res << endl;
	std::string nameKey = "lastName";
	std::string nameValue = "Watson";
	file.set(nameKey, nameValue);
	//Attribute att;
	//cout << att.objectIsEmpty() << endl;
	//file.print();
	std::vector<Attribute*> res = file.search("lastName");
	for (int i = 0; i < res.size(); i++)
	{
		cout << res[i]->getValue() << endl;
	}
	*/
	return 0;
}